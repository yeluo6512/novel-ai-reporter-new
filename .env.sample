# Copy this file to `.env` and update values for your environment.
# Lines that remain commented use the application's built-in defaults.

# Application metadata
# APP_APP_NAME=CTO Agents Backend
# APP_APP_VERSION=0.1.0
APP_ENVIRONMENT=development

# Provider integration (optional)
# Uncomment and configure when calling external providers.
# APP_PROVIDER_BASE_URL=https://api.example.com
# APP_PROVIDER_API_KEY=replace-with-api-key

# Cross-origin resource sharing
# Provide a comma-separated list. Leaving this empty allows all origins.
APP_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Filesystem layout
# Paths can be absolute or relative to the repository root.
# APP_BASE_DIR=./runtime
# APP_CONFIG_DIR=./runtime/config
# APP_DATA_DIR=./runtime/data
# APP_PROJECTS_SUBDIR=projects
# APP_AGENTS_MANIFEST_NAME=agents.md
